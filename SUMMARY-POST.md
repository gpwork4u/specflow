# 一篇總結（單則長文版）

> 自包含的單篇短文，可直接當一則 Threads / LinkedIn 長貼，或 blog 摘要。

---

## 我請 AI 幫自己的開發流程跑了 7 輪實驗

上個月我做了一套讓 AI 自己跑開發的多 agent 流程（SpecFlow）：從寫 spec、技術規劃，到 backend / frontend / QA / 設計多個 agent 平行實作，全部以 GitHub issue 和 PR 協作。

做完之後我一直在憑感覺修 —— prompt 太囉嗦就砍、agent 漏步就補規則、token 好像吃多就再瘦。直到某天我停下來問自己：我到底有沒有真的讓它變好，還是只是「感覺」變好？

我發現我從來沒認真量過它的數據。於是我請 Claude Code 幫我寫一份 benchmark 實驗，連續跑了 7 輪，每輪建一個全新的 demo 專案讓流程跑到底，量總 token、量產出幾個能用的 PR、翻 log 找根因，再改一個東西重跑。

幾個顛覆我認知的發現：

**一、我把 prompt 瘦身 79%，總 token 幾乎沒省。** 因為 prompt cache 把多數收益吃掉了，真正的成本在 output 和工具 I/O。該看的不是「每次跑多少 token」，是「每個交付物多少 token」。

**二、第一輪 4 個 agent 把 code 都寫好了，卻一個 PR 都沒開。** 翻 log 才懂——它們都在「正要 commit」那刻被系統上限切斷，預算全花在做事，沒走到交付。解法是反轉順序：先開好 draft PR，再寫 code，撞牆也保底。

**三、最關鍵的一課**：要 agent 穩定做對一件事，不是寫更兇的 prompt，是把流程壓成它「不可能做錯」的單一動作。我把開分支、commit、push、開 PR 包成一支 script，agent 跑一行搞定，想跳步都沒辦法，可靠度從 50% 拉到接近 100%。

七輪下來，PR 產出率從 0% 穩定到 100%。但最大的收穫不是修好了流程，是心態的轉變：

**調 AI 流程不是「寫 prompt 的藝術」，是可靠度工程。** 沒有 benchmark 就沒有優化，只有自我安慰；失敗了就翻 log 找根因，跟 debug 分散式系統一樣；接受 agent 會犯錯，重點是設計結構性防呆，而不是寄望它每次都聰明。

如果你也在調自己的 AI 工作流，我最想分享的一句話是：**先別急著改 prompt，先想辦法量它。** 你會很驚訝有多少「我以為的優化」其實沒效果，又有多少真正的問題藏在你從沒看過的 log 裡。

完整紀錄：github.com/gpwork4u/specflow
